/** Los tipos de `la-base-va-al-dia.mjs`, para que su prueba se compruebe entera. */
export declare function laUltimaDelCodigo(ficheros: readonly string[]): number;
export declare function veredicto(
  delCodigo: number,
  deLaBase: number | null,
): { readonly para: boolean; readonly frase: string };
