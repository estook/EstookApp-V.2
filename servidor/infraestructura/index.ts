/**
 * servidor/infraestructura · repositorios, Postgres y almacenamiento.
 *
 * La unica capa que sabe que por debajo hay Postgres. Implementa los puertos que declara servidor/aplicacion.
 *
 * Vacio a proposito en M0. Entra en M2.
 */
export const CAPA = 'infraestructura' as const;
