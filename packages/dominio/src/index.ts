/**
 * @estook/dominio · tipos, reglas puras y calculos. Sin red y sin base de datos.
 *
 * Vacio a proposito en M0: el modelo maestro de alcances, roles y permisos entra
 * en M1, y los motores de dinero, unidades, coste, fiscal y tiempo en M2.
 * Escribir aqui antes de tiempo es exactamente lo que prohibe la regla 1.
 */
export const PAQUETE = '@estook/dominio' as const;
